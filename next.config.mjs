/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['d1csarkz8obe9u.cloudfront.net'], // Add the external hostname here
    },
  };
  
  export default nextConfig;