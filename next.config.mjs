const nextConfig = {
  images: {
    domains: ['d1csarkz8obe9u.cloudfront.net', 'avatars.githubusercontent.com'], // Add the external hostname here
  },
};

export default nextConfig;// Use export default instead of module.exports
